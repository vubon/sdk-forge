# {{.DisplayName}} PHP SDK

{{.Description}}

**Version:** {{.SDKVersion}}

## Installation

Install via Composer:

```bash
composer require vendor/{{.SDKName}}
```

Or add to your `composer.json`:

```json
{
    "require": {
        "vendor/{{.SDKName}}": "{{.SDKVersion}}"
    }
}
```

## Quick Start

```php
<?php

require_once __DIR__ . '/vendor/autoload.php';

use {{.Namespace}}\{{.ClientClassName}};
{{- if .HasAPI}}
{{.APIImports}}
{{- end}}

// Initialize client
$client = new {{.ClientClassName}}(
    baseUrl: "{{.BaseURL}}"
{{- if .HasAuth}}
    options: [
{{.AuthExample}}
    ]
{{- end}}
);

{{- if .HasOperations}}
// Use API methods
{{.UsageExample}}
{{- end}}
```

## HTTP Library

This SDK uses **{{.HTTPLib}}** for HTTP requests.

{{- if .HasAuth}}
## Authentication

Configure authentication when creating the client:

```php
{{.AuthExampleCode}}
```
{{- end}}

{{- if .RetryEnabled}}
## Retry Mechanism

This SDK includes automatic retry logic for transient failures.

**Configuration:**
- Max attempts: {{.RetryMaxAttempts}}
- Strategy: {{.RetryStrategy}}
- Initial delay: {{.RetryInitialDelay}}s
- Max delay: {{.RetryMaxDelay}}s
- Backoff multiplier: {{.RetryBackoffMultiplier}}

{{- end}}
## Testing

Run PHPUnit tests:

```bash
composer install --dev
vendor/bin/phpunit
```

## Code Quality

This SDK includes code quality tools:

- **PHP_CodeSniffer** (PSR-12): `vendor/bin/phpcs`
- **PHPStan** (static analysis): `vendor/bin/phpstan analyse`
- **PHP-CS-Fixer** (formatting): `vendor/bin/php-cs-fixer fix`

## License

Generated by [SDK Forge](https://github.com/vubon/sdk-forge)
